[Unit]
Description=Resizes a flaarum instance
Wants=network.target
After=network.target

[Service]
Type=simple
User=root
ExecStart=/opt/flaa103/resizer
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target